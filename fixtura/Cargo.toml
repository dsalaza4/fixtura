[package]
name = "fixtura"
version = "0.8.1"
edition = "2021"
description = "Declarative fake data injection for Rust tests. Built on fake-rs."
license = "MIT"
repository = "https://github.com/dsalaza4/fixtura"
readme = "../README.md"
keywords = ["testing", "fake", "fixtures", "macros", "dummy"]
categories = ["development-tools::testing"]

[dependencies]
fixtura-macros = { path = "../fixtura-macros", version = "0.8.1" }

[dev-dependencies]
fake = { version = "5", features = ["derive"] }
tokio = { version = "1", features = ["rt", "macros"] }
trybuild = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
